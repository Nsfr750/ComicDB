[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pillow = ">=10.0.0"
pandas = ">=2.0.0"
mysql-connector-python = ">=8.0.0"
comicapi = ">=3.2.0"
unrar = ">=0.4"
rarfile = ">=4.0"
pikepdf = ">=8.0.0"
python-magic = ">=0.4.27"
python-magic-bin = ">=0.4.14"
ttkbootstrap = ">=1.10.1"
black = ">=23.0.0"
mypy = ">=1.0.0"
pytest = ">=7.0.0"
sphinx = ">=6.0.0"
sphinx-rtd-theme = ">=1.2.0"
flake8 = ">=6.0.0"
isort = ">=5.12.0"
requests = "*"

[dev-packages]

[requires]
python_version = "3.12"
